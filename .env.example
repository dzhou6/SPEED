
# MongoDB (Atlas or local)
MONGO_URI=...
MONGO_DB=coursecupid

# Frontend -> Backend base URL (Vite reads VITE_* at runtime for dev)
VITE_API_BASE_URL=http://localhost:8000

# Backend port (compose uses it)
BACKEND_PORT=8000
